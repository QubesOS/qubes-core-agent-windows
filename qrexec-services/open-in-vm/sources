TARGETNAME = open-in-vm
TARGETTYPE = PROGRAM
TARGETPATH = bin

UMTYPE = console
USE_MSVCRT = 1

UMENTRY = wmain
C_DEFINES = $(C_DEFINES) /D_UNICODE /DUNICODE

INCLUDES = $(INCLUDES); \
    $(SDK_INC_PATH)\..\ddk; \
    ..\filecopy-common; \
    ..\..\include; \
    $(QUBES_INCLUDES);
    
SOURCES = qopen-in-vm.c version.rc

TARGETLIBS = \
    $(SDK_LIB_PATH)\User32.lib \
    ..\libs\*\filecopy-common.lib \
    $(QUBES_LIBS)\windows-utils.lib
