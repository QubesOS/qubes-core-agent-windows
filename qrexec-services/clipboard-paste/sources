TARGETNAME=clipboard-paste
TARGETTYPE=PROGRAM
TARGETPATH=bin

UMTYPE=windows
USE_MSVCRT=1

UMENTRY = wwinmain
C_DEFINES = $(C_DEFINES)  /D_UNICODE /DUNICODE

INCLUDES=$(INCLUDES); \
                    $(SDK_INC_PATH)\..\ddk; \
                    ..\include; \
                    ..\..\include; \
                    $(QUBES_INCLUDES);

SOURCES=clipboard-paste.c

TARGETLIBS=\
        $(SDK_LIB_PATH)\kernel32.lib \
        $(SDK_LIB_PATH)\user32.lib \
        $(SDK_LIB_PATH)\shlwapi.lib \
        $(QUBES_LIBS)\windows-utils.lib \
        $(SDK_LIB_PATH)\userenv.lib
