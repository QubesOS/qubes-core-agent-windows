TARGETNAME = vm-file-editor
TARGETTYPE = PROGRAM
TARGETPATH = bin

UMTYPE = console
USE_MSVCRT = 1

UMENTRY = wmain
C_DEFINES = $(C_DEFINES) /D_UNICODE /DUNICODE

INCLUDES = $(INCLUDES); \
    $(SDK_INC_PATH)\..\ddk; \
    ..\filecopy-common; \
    ..\..\include; \
    $(QUBES_INCLUDES);
    
SOURCES = vm-file-editor.c version.rc

TARGETLIBS = \
    ..\libs\*\filecopy-common.lib \
    $(QUBES_LIBS)\windows-utils.lib \
    $(SDK_LIB_PATH)\Shell32.lib \
    $(SDK_LIB_PATH)\rpcrt4.lib
