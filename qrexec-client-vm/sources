TARGETNAME=qrexec-client-vm
TARGETTYPE=PROGRAM
TARGETPATH=bin


UMTYPE=console
USE_MSVCRT=1

UMENTRY = wmain
C_DEFINES = $(C_DEFINES)  /D_UNICODE /DUNICODE

INCLUDES=$(INCLUDES); \
                    $(SDK_INC_PATH)\..\ddk; \
                    ..\include; \
                    $(QUBES_INCLUDES);

SOURCES=qrexec-client-vm.c

TARGETLIBS=\
            $(SDK_LIB_PATH)\kernel32.lib \
            $(SDK_LIB_PATH)\shlwapi.lib \
            $(QUBES_LIBS)\windows-utils.lib
